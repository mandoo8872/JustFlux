/**
 * JustFlux v2 — Client-Only PDF Editor
 * Local-only, page-centric, no server calls
 */

import { Shell } from './ui/layout/Shell';

function App() {
  return <Shell />;
}

export default App;
